name: CI

on:
  pull_request:
    branches:
      - main
      - "phase/**"
  push:
    branches:
      - main
      - "phase/**"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: "${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}"
  cancel-in-progress: true

jobs:
  repository-policy:
    name: Repository policy
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Validate public instructions and tracked content
        shell: pwsh
        run: ./scripts/validate_instructions.ps1
      - name: Reject whitespace errors
        run: git show --check --oneline --no-renames HEAD

  python:
    name: "Python (${{ matrix.os }})"
    runs-on: "${{ matrix.os }}"
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install locked Python toolchain
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: 0.12.0
          python-version: "3.14.4"
          enable-cache: true
          cache-dependency-glob: uv.lock
      - name: Synchronize locked dependencies
        run: uv sync --locked --all-groups
      - name: Audit locked Python environment
        run: uv run python scripts/verify_python_dependencies.py
      - name: Check Python formatting
        run: uv run ruff format --check .
      - name: Lint Python
        run: uv run ruff check .
      - name: Check Python types
        run: uv run pyright
      - name: Enforce import boundaries
        run: uv run paritygrid-check-boundaries
      - name: Check generated OpenAPI contract drift
        run: uv run python scripts/export_openapi.py --check
      - name: Check generated TypeScript boundary drift
        run: uv run python scripts/generate_api_types.py --check
      - name: Run Python tests and coverage
        run: uv run pytest --cov=paritygrid --cov-report=term-missing
      - name: Enforce scoped Python branch coverage
        run: uv run python scripts/verify_python_coverage.py
      - name: Verify isolated wheel and spawned-process import
        run: uv run python scripts/verify_wheel_install.py
      - name: Run backend smoke check
        run: uv run paritygrid smoke

  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: web
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: web/package-lock.json
      - name: Install locked frontend dependencies
        run: npm ci
      - name: Check frontend formatting
        run: npm run format:check
      - name: Lint frontend
        run: npm run lint
      - name: Check frontend types
        run: npm run typecheck
      - name: Compile generated TypeScript declaration
        run: npm run generate:api:compile
      - name: Run frontend tests and coverage
        run: npm run test:coverage
      - name: Build production frontend
        run: npm run build
      - name: Install Chromium for shell-browser coverage
        run: npm exec -- playwright install --with-deps chromium
      - name: Run Chromium shell-browser coverage
        run: npm run test:browser
      - name: Verify committed frontend distribution drift
        working-directory: .
        run: python scripts/verify_frontend_dist.py
      - name: Audit frontend dependencies
        run: npm audit --audit-level=high

  frontend-api-smoke:
    name: Frontend API smoke
    needs:
      - python
      - frontend
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install locked Python toolchain
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: 0.12.0
          python-version: "3.14.4"
          enable-cache: true
          cache-dependency-glob: uv.lock
      - name: Install Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: web/package-lock.json
      - name: Synchronize locked dependencies
        run: uv sync --locked --all-groups
      - name: Install locked frontend dependencies
        working-directory: web
        run: npm ci
      - name: Exercise Vite-to-FastAPI proxy
        run: uv run python scripts/verify_frontend_api_path.py
